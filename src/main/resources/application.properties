# MySQL Database
spring.datasource.url=jdbc:mysql://localhost:3306/chatdb?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=hemanat123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Server port
server.port=8082
```

\u26A0\uFE0F Replace `YOUR_MYSQL_PASSWORD_HERE` with your actual MySQL root password. If you have no password, leave it empty.

---

### Step 4 \u2014 Restart Spring Boot

After saving, restart the app in Eclipse. Watch the console \u2014 it should say something like:
```
Tomcat started on port 8081

spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB